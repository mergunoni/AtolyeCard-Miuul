# Copy to .env.local and fill in. .env.local is git-ignored.
# Every webhook event (order_request, card_saved, meeting_request) POSTs here;
# the `event` field in the payload tells them apart.
VITE_WEBHOOK_URL=https://example.com/webhook/atolyekart
